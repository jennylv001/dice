{
  "name": "@kismet/worker",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config ../infra/wrangler.toml",
    "dry-run": "wrangler deploy --dry-run --config ../infra/wrangler.toml --outdir=dist",
    "deploy": "wrangler deploy --config ../infra/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "4.45.4",
    "typescript": "^5.6.3",
    "@cloudflare/workers-types": "^4.20241011.0"
  }
}
