# Copy to .env and adjust.
# When deploying Pages without a Worker route on the same hostname, point to the worker's workers.dev or custom domain.
VITE_API_BASE="https://kismet-worker.devv-handler.workers.dev"
